import {deviceStorage} from "../storage/storage";import {SpendingModel} from "../store/Type/models";export const dateFormat = (date: Date): string => {    return date.toLocaleDateString()};export const getDateFormatTime = (date: Date): string => {    return date.toLocaleTimeString('ru-RU');};export const replaceCommaOnDot = (value: string): string => {    return String(parseFloat(value.replace(/,/g, '.')))}export const convertToDate = (dateString: string): Date => {    return new Date(dateString);};export const generateColor = () => {    const randomColor = Math.floor(Math.random() * 16777215)        .toString(16)        .padStart(6, '0');    return `#${randomColor}`;};export const getToken = async () => {    return await deviceStorage.getItem('token')}export const getTotalSpends = (walletHistory: SpendingModel[]): number => {    const totalSpends = Math.round(walletHistory?.reduce((acc, curr) => {        return acc + Number(curr.amount)    }, 0))    return totalSpends ?? -1}